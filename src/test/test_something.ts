
import * as assert from 'assert';

describe('Example test', () => {
    describe('Example feature', () => {
        it('should be testable!', () => {
            assert.equal(1, 1); // Better be true :)
        });
    });
});

